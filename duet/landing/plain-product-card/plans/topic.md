Read ../../BRIEF.md; the developed Plain Product Card write-up at the end of ../conversation.md (the canonical spec you are descending from); the final verdict at the end of ../../judging/conversation.md; and, because your output must serve both verdict branches, the pricing-table treatment in the Design-Partner Compact write-up at the end of ../../design-partner-compact/conversation.md (its section 2 imports a plan table whose job differs: the map that bounds the compact, not itself the trust argument).

This conversation takes Card section 3 — plans and commercial boundaries — to screen level. The parent write-up calls it the page's center of gravity: the pricing table itself is the trust argument. The bar: a builder could implement it without asking "what does that mean?", and Victor can close his eyes and see the screen.

Produce, for the section:

- The exact element inventory and layout in prose: the four plans, the per-plan fact rows (price; where it runs; model terms; what is included; access reality today), their order and relative prominence, and how equal-prominence-with-price is achieved structurally. No pixel values, no ribbons, no anchoring devices.
- The plan copy, drafted against docs/billing_design.md — verify every number, limit, and term against the repo; no fact ships on memory. Converge on exact wording where you can (mark it FINAL). The access-reality values themselves are build-time facts: spec the exact vocabulary for each possible state (purchasable now / manually provisioned / request access / sales-led) and the verification step that selects among them, rather than guessing today's state.
- The Branch-A variant note: what changes when this table serves the Compact's section 2 (the scope statement beneath it, Team's entry-through-program line), so the table's structure survives both branches without redesign.
- What must be verified at build time, as a checklist (including the Stripe-live-mode dependency on the LLC filing).

Copy discipline: the brief's never-AI-generated criterion is a hard constraint. No per-plan marketing adjectives, no "most popular", nothing that argues rather than states. Each fact appears once.

End with a standalone screen spec for the section.

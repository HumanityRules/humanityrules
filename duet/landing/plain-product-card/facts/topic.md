Read ../../BRIEF.md; the developed Plain Product Card write-up at the end of ../conversation.md (the canonical spec you are descending from, including the turns where the credential-screen limits were verified against the real templates); and the final verdict at the end of ../../judging/conversation.md.

This conversation takes Card section 2 — product facts — to screen level. The bar for everything you produce: a builder could implement it without ever asking "what does that mean?", and Victor can close his eyes and see the screen.

Produce, for the section:

- The exact element inventory and layout in prose: the spec-account structure, its ordering by importance, where the credential supporting figure sits, where the Hermes provenance line sits, where the current-uses line sits. No pixel values, no medium/technique decisions.
- The facts themselves, drafted: the always-available agent and hosted runtime with persistent state; credential handling at exactly the level the current interface supports; the deployment boundary (hosted tiers vs customer AWS); "built on the open-source Hermes harness" as identification only. Converge on exact wording where you can (mark it FINAL); direction elsewhere. Verify every fact you draft against the repo — no fact ships on memory.
- The current-uses line: specify the verification protocol precisely (what counts as a verifiable task category from the seven friends-and-family deployments, who collects it, and the omission rule if verification fails). Draft the line's form with placeholder categories, clearly marked as unverified until collection happens.
- Exact asset spec for the credential-screen capture, with privacy-pass criteria.
- What must be verified at build time, as a checklist.

Copy discipline: the brief's never-AI-generated criterion is a hard constraint. Datasheet register: dense, exact, zero adjectives of reassurance, no sentence arguing the page's honesty. Every named mechanism replaces a genre security adjective.

You may inspect the repo's actual templates, views, and docs to ground every fact. End with a standalone screen spec for the section.

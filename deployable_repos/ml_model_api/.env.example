# S3 Configuration for model storage
S3_BUCKET=your-ml-models-bucket
S3_MODEL_KEY=models/iris_classifier.joblib
AWS_REGION=us-east-1

# Model loading source: "s3" or "local"
MODEL_SOURCE=local

# Local model path (used when MODEL_SOURCE=local)
LOCAL_MODEL_PATH=sample_model.joblib

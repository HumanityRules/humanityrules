# Contributing
1) Fork & branch from `main` (e.g. `feat/xyz`).  
2) `make install && make dev` to run locally; `make test`, `make lint` before PR.  
3) Keep PRs small and focused.  
4) Use Conventional Commits (`feat:`, `fix:`, `chore:`...).  
5) Security-sensitive issues: see SECURITY.md.

# DynamoDB table for storing active WebSocket connections
# Leave empty for local development (uses in-memory storage)
DYNAMODB_CONNECTIONS_TABLE=

# DynamoDB table for storing chat messages
# Leave empty for local development (uses in-memory storage)
DYNAMODB_MESSAGES_TABLE=

# AWS region for DynamoDB
AWS_REGION=us-east-1

# Application port
PORT=8000

[project]
name = "doh-pdp-mock-lambda"
version = "0.1.0"
description = "Mock PDP used by the policy-proxy end-to-end test."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

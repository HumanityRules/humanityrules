"""
Deployment agent service.

This module provides the AI agent that guides users through deployments.

Note: We don't import submodules at the top level to avoid Django initialization
issues when running standalone scripts like the repo_analysis test harness.
"""

--- a/run_agent.py
+++ b/run_agent.py
@@ -10990,2 +10990,3 @@
-                # Notify progress callback of model's thinking (used by subagent
-                # delegation to relay the child's reasoning to the parent display).
+                # Notify progress callback of delegated child progress only.
+                # Top-level reasoning is delivered by structured reasoning callbacks;
+                # assistant content is the final answer, not hidden thinking.
@@ -10994 +10995 @@
-                    # Strip reasoning XML tags that shouldn't leak to parent display
+                    # Strip reasoning XML tags that shouldn't leak to parent display.
@@ -10998,2 +10999 @@
-                    # For subagents: relay first line to parent display (existing behaviour).
-                    # For all agents with a structured callback: emit reasoning.available event.
+                    # For subagents: relay first line to parent display (existing behavior).
@@ -11006,5 +11005,0 @@
-                    elif _think_text:
-                        try:
-                            self.tool_progress_callback("reasoning.available", "_thinking", _think_text[:500], None)
-                        except Exception:
-                            pass
